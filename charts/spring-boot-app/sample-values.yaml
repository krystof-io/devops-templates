replicaCount: 1

image:
  repository: docker-private.krystof.io/event-bus
  tag: "1.0.0"
  pullPolicy: Always

imagePullSecrets:
  - name: docker-private-ro

nameOverride: ""

springBoot:
  profiles:
    active: "dev"

traefikIngressRoute:
  enabled: true
  matchHost: "event-bus.dev.krystof.io"
